<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Portal - Performance Dashboards</title>
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%
    #
    # Production mode or not required for webpack-dev-server mode
    #
    if !(Rails.env.development? && ENV['DEV_SERVER'] == 'true') %>
      <link rel="stylesheet" href="/stylesheets/portal.css"/>
  <% end %>
</head>
<body class="portal <%= body_class(yield(:body_class_names)) %>">
  <div class="phase-banner">
    <div class="wrapper">
      <span>This is a working alpha and has limited functionality. Service data shown is
        accurate as at the time it was published.</span>
    </div>
  </div>
  <header class="main__header">
    <div class="wrapper">
      <a href="/portal" title="Go to the GOV.AU Portal's homepage" class="logo">
        <img src="<%= image_path('gov-performance.svg') %>" alt="Logo of GOV.AU Performance Dashboard" height="30px" />
      </a>
    </div>
  </header>
  <div>
    <div class="wrapper">
      <%= yield %>
    </div>
  </div>
  <footer class="main__footer">
    <div class="wrapper">

    </div>
  </footer>
  <%
    #
    # Production mode or webpack-dev-server mode
    #
    if !(Rails.env.development? && ENV['DEV_SERVER'] == 'true') %>
    <script src="/javascripts/portal.js"></script>
  <% else %>
      <script src="http://localhost:8080/javascripts/portal.js"></script>
  <% end %>
</body>
</html>
